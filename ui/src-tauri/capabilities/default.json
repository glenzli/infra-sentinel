{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Only exposes the narrow Infra Agent bridge to the dashboard.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
